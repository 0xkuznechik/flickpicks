TODO

  general
  - sort out how prisma and the db work, esp with docker

  front page
  - update dates dynamically

  ballot
  - lock the top banner
  - display more vertically

  - each selection should have an input for amt to allocate, odds, then amount
    that will be won
  - ability to lock a specific pick

  - add odds to the ballot

NOTES

Commands

# Linter
npx prettier --write path/to/your/file.ts

# "Compile" file
npx tsc yourfile.ts --noEmit

# "Compile" project
npx tsc --project tsconfig.json --noEmit
